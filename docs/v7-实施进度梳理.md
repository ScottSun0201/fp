# v7 · 对账闭环实施进度梳理（当前状态一览）

> 分支：reconcile-v2 ｜ 更新：2026-08-18
> 本文档回答三个问题：**我们到哪了 / 还剩什么 / 下一步怎么走**

---

## §1 一句话总览

> **方案已定稿（v4/v5/v6），P0 的地基已打好，主链路还没接通。**
> 现在处于"地基完成、等开工"的状态，随时可以继续。

---

## §2 当前进度（已完成 ✅）

### 方案文档（全部定稿并推送）

| 文档 | 内容 | 状态 |
|------|------|------|
| v1 | 项目全量分析（架构/解析/匹配/快递/问题清单） | ✅ |
| v2 | 业务流程梳理（现状流程 + 7 个问题点 + 6 个待拍板） | ✅ |
| v3 | v2 方案结果（6 项决定 + P0/P1/P2 功能清单） | ✅ |
| v4 | 闭环设计（4 层闭环 + 6 问验收清单） | ✅ |
| v5 | 实施方案（现状断点解析 + 落地清单） | ✅ |
| v6 | P0 实施规格（F1~F5 详细规则 + 验收标准） | ✅ |

### 代码（F1 地基，已提交推送）

| 内容 | 说明 | 状态 |
|------|------|------|
| 修 `models.py` 垃圾代码 | 删除损坏尾部 + SQLite 残留，语法验证通过 | ✅ commit `0848d26` |
| 新增 `stm_diff_ticket` 工单表 | 闭环核心表：状态机字段、跨月标记、处理动作全齐 | ✅ commit `0ad968f` |
| 对账重算时同步清理工单 | 删 allocation 时同步删本地工单，**保留跨月带入工单** | ✅ commit `0ad968f` |

---

## §3 剩余工作（未完成 ⏳）

### F1 主链路剩余 4 步（后端）

| # | 步骤 | 做什么 | 位置 |
|---|------|--------|------|
| 1 | **工单自动生成** | allocation 状态 `!= PASS`（OVER/INFO/超额）时自动建工单，按 `statement_id+line_key+diff_type` 去重 | `app.py` 的 `_sync_statement_allocations` 插入之后 |
| 2 | **核销校验** | 点"确认对账"时查未解决工单，有则要求填差异说明（force 才放行） | `app.py` `approve_statement_107` |
| 3 | **跨月带入 API** | 上月未解决工单 → 复制为下月工单（is_carried_forward=1） | 新增路由 |
| 4 | **差异清单 API** | 列表查询 + 状态流转 + 处理动作更新 | 新增路由 |

### F2 差异清单页（前端）

- 现有"逐行处理"页升级为"差异清单"，默认只看问题行
- 字段：工单号/供应商/行号/差异金额/原因/状态/负责人/时间
- 筛选：状态、供应商、对账任务、月份

### F3 状态机 / F4 核销校验 / F5 跨月带入

- F3 状态流转（待处理→处理中→已解决/遗留）依赖清单页动作 → F2 之后
- F4 依赖 F1 第 2 步（核销校验接口）
- F5 依赖 F1 第 3 步（跨月带入接口）

---

## §4 依赖关系图

```
F1 工单生成（1-4步）
   ├─→ F4 核销校验（F1-2 完成后即可）
   ├─→ F5 跨月带入（F1-3 完成后即可）
   └─→ F2 差异清单页（F1-4 API 就绪后做前端）
          └─→ F3 状态机（依赖 F2 页面的操作按钮）
```

**建议顺序：F1(1→2→3→4) → F2 → F3 → F4 校验 → F5 跨月。**

---

## §5 下一步（等你点头）

| 选项 | 内容 | 工作量 |
|------|------|--------|
| **A. 继续 F1** | 把工单自动生成 + 核销校验 + 跨月 API 接通，后端闭环通 | 约半天 |
| **B. 先看效果再继续** | 先在 107 部署当前版本，手动造一张有差异的对账单，确认工单表建好 | 约 1 小时 |
| **C. 停在这里** | 方案和地基都留档，等业务上再有输入再动 | — |

> 之前未确认的 3 个业务拍板点（v6 §0）：处理动作 5 类 / 财务可标遗留 / 允许带说明核销 —— 若不认可，改动成本低，随时可调。

---

## §6 代码状态备忘

```
分支 reconcile-v2 @ 0ad968f（已推送 origin）
工作区：干净 ✅
未推送：无 ✅
```
